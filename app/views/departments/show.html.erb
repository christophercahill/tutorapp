<% @courses.each do |course| %>
                      <% 
    user_id = post.user_id %>
   <% @user = User.find(user_id) %>
  <%  @profile = @user.profile %>
                    <h2><%= link_to course.title, course_path(course), class: 'link-to-text-color' %>
                        <br>
                        <small><%= @profile.name%></small>
                    </h2>
                    <p><%= course.description %></p>
                    
                    <hr>
                </div>
                <% end %>